{
  "name": "pideck-workspace",
  "private": true,
  "packageManager": "yarn@4.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @pideck/client start",
    "web": "node tools/start-web.mjs",
    "android": "yarn workspace @pideck/client android",
    "ios": "yarn workspace @pideck/client ios",
    "desktop": "yarn workspace @pideck/desktop dev",
    "runtime:start": "node --experimental-strip-types apps/server/src/main.ts serve",
    "runtime:check": "yarn workspace @pideck/engine check && yarn workspace pideck check",
    "lint": "yarn workspace @pideck/client lint",
    "test": "yarn workspace @pideck/client-sdk test && yarn workspace @pideck/ui test && yarn workspace @pideck/engine test && yarn workspace pideck test",
    "api:generate": "yarn workspace @pideck/api-contract generate && yarn workspace @pideck/client-sdk api:generate",
    "api:check": "yarn workspace @pideck/api-contract validate",
    "web:build": "yarn workspace @pideck/client build",
    "build:prod": "yarn web:build && yarn runtime:check"
  }
}
