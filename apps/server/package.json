{
  "name": "pideck",
  "version": "0.1.0",
  "description": "PiDeck embedded Pi SDK runtime and companion plugin",
  "keywords": [
    "pi-package"
  ],
  "type": "module",
  "bin": "bin/pideck.cjs",
  "pi": {
    "extensions": [
      "./src/extension/index.ts"
    ]
  },
  "files": [
    "src",
    "bin",
    "public",
    "README.md"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@pideck/engine": "workspace:*",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2"
  }
}
