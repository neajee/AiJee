import { memo } from "react";
import { View } from "tamagui";

interface DiffSidebarProps {
  messages: unknown[];
}

export const DiffSidebar = memo(function DiffSidebar(
  _props: DiffSidebarProps,
) {
  return <View />;
});
